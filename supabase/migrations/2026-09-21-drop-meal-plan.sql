-- The 7-day meal-plan calendar and "daily extras" were removed from the app
-- (see the This week buckets in week_items). Both tables were empty.
drop table if exists daily_extras;
drop table if exists meal_plan;
